  int rotated_search(vector<int>& nums, int val) {
    // find pivot idx 
    // Implementation of pivot depends on weather duplicates are present
    // search(0, p_idx - 1);
    // search(p_idx, n - 1);
}